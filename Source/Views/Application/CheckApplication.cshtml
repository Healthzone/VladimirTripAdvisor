@model IEnumerable<VladimirTripAdvisor.Models.ApplicationModelDB>
@{
    ViewData["Title"] = "CheckApplication";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Список заявок на улучшение статуса аккаунта</h2>
        </div>
    </div>

    <br />

    @if(Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Фамилия
                    </th>
                    <th>
                        Имя
                    </th>
                    <th>
                        Статус заявления
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {   if(obj.ApplicationStatus == Logic.Application.ApplicationStatus.Created)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">Создана</td>
                            <td></td>
                        </tr>
                    }
                    else if (obj.ApplicationStatus == Logic.Application.ApplicationStatus.Taked)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">@obj.ApplicationStatus</td>
                            <td></td>
                        </tr>
                    }
                    else if (obj.ApplicationStatus == Logic.Application.ApplicationStatus.Processing)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">@obj.ApplicationStatus</td>
                            <td></td>
                        </tr>
                    }
                    else if (obj.ApplicationStatus == Logic.Application.ApplicationStatus.Edit)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">@obj.ApplicationStatus</td>
                            <td></td>
                        </tr>
                    }
                    else if (obj.ApplicationStatus == Logic.Application.ApplicationStatus.Done)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">@obj.ApplicationStatus</td>
                            <td></td>
                        </tr>
                    }
                    else if (obj.ApplicationStatus == Logic.Application.ApplicationStatus.Canceled)
                    {
                        <tr>
                            <td width="25%">@obj.User.Surname</td>
                            <td width="25%">@obj.User.Name</td>
                            <td width="25%">@obj.ApplicationStatus</td>
                            <td></td>
                        </tr>
                    }

                }
            </tbody>
        </table>
    }
    else
    {
        <p>Список пустой</p>
    }
</div>

